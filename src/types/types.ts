export type PersonalDetailsType = {
    name: string;
    title: string;
    contact: string;
    email: string;
    location: string;
};

export type ExperienceProps = {
    company: string;
    location: string;
    position: string;
    duration: string;
    description: string;
};

export type EducationType = {
    school: string;
    location: string;
    degree: string;
    year: string;
};
